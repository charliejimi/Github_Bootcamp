# 變更紀錄

本檔案記錄待辦清單 App 各版本的功能變動。

## [0.1.0] - Step 1

### 新增

- 新增待辦事項，並阻止空白內容加入清單。
- 勾選待辦事項以標記完成，完成後文字顯示刪除線並淡化。
- 刪除個別待辦事項。
- 顯示整份清單的未完成項目數量。
- 使用 `localStorage` 保存待辦資料，重新整理頁面後仍可保留。

### 變更

- 清單沒有項目時，顯示空狀態提示文字。
- 介面採用置中卡片版面，並支援手機螢幕。

## [0.2.0] - Step 2

### 新增

- 在標題列新增深色模式切換按鈕，可在淺色與深色模式之間切換。
- 使用 `localStorage` 記住使用者的主題偏好，重新整理後仍維持設定。
- 使用者尚未手動選擇主題時，透過 `prefers-color-scheme` 跟隨系統設定。
- 新增「全部」、「未完成」、「已完成」三種清單篩選模式。
- 篩選結果為空時，依目前模式顯示對應提示文字。

### 變更

- 深色與淺色配色統一透過 CSS 變數管理。
- 底部未完成計數改為始終依完整清單計算，不受篩選條件影響。